export const generateBaseUrl = (limit = 30, offset = 0) => {
	const baseUrl = 'https://pokeapi.co/api/v2/pokemon/';

	return `${baseUrl}?limit=${limit}&offset=${offset}`;
};

export const renderPokemonTypes = (pokemonTypes) => {
	switch (key) {
		case value:
			break;

		default:
			break;
	}
};
